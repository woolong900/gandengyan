---
description: 左右家手牌用 APK 预渲染长方体槽位
globs: game/src/ui/{layout,render,sideWall3d}.ts
alwaysApply: true
---

# 左右家手牌（3D 牌墙）

APK CardLayer3D 的 `left_hand_hide` / `right_hand_hide`：每张是预渲染长方体（`zlp_1..14` / `ylp_1..14`），按预制体中心点摆。绿背朝桌心，象牙顶和近端侧面已经画在贴图里。

- 槽位在 `SIDE_HAND`，绘制在 `sideWall3d.ts`。
- 满手从远端对齐；摸牌用独立的 `drawn` 槽。
- **禁止**把 `tile_back_up` 仿射贴成一块扁四边形。
- **禁止**对 `tile_side` / `tile_back_up` 整列 `rotate`、错切，或把 `tile_back_up` 转 90° 当左右立牌。

# 四家碰杠

四家同一套路：`{left,right,up,down}_gang_show` 的 Sprite 是 `sizeMode: RAW`，每个槽位一张独立的
预渲染长方体（明牌 左 `zpg*` / 右 `ygp*` / 对家 `spg*` / 自家 `xpg*`，暗杠 `zag*` / `yag*` /
`sag*` / `xag*`，每套 16 张）。透视和厚度都已经画进贴图，按 `SIDE_MELD` 的槽位中心 1:1 摆上去即可。

- **禁止**用扁平的 `tile_meld` 充当任何一家的碰杠牌身。它没有立体侧面，
  自家看过去会是「直直的」，而 `xpg*` 能看到最左侧那张牌的左侧面。
- **禁止**把自家的 `tile_meld` 转 180° 当对家的碰杠。对家要用 `spg*`：绿边在**下**，
  才是从自己玩家视角看过去的样子；转 180° 会把绿边翻到上面，看着像从对面视角看的。
- 对家的牌面字不是靠旋转，而是 card 节点的**负缩放** `(-0.32, -0.21)`。
- 自家手牌居中、碰得越多越窄，所以自家碰杠区右缘要顶着**实际**手牌左缘
  （`bottomMeldShift`），不能锚在满 13 张时的左缘，否则会被挤出屏幕。

- **禁止**把矩形的 `bg_left_right_g`（`tile_meld_side`）拉伸进槽位尺寸，再用错切伪造斜边。
  牌身节点在预制体里没有任何 skew／rotate，斜边不是算出来的。
- 牌面字是 card 子节点，单独变换：euler.z 左 −90° / 右 +90°，再按牌面剪切（见「牌面字的剪切」）。
  这个形变只作用于字，**不要**把牌身的形变叠加上去，否则字会二次倾斜。
- 绘制顺序照预制体子节点：远→近，杠的第 4 张最后。
- 贴图名的编号和预制体槽位名的编号**是反的**：`left_gang_show/card_1_1` 用的是 `zpg4_3`，
  `up_gang_show/card_1_1` 用的是 `spg4_3`（自家组号对得上、组内序号反）。所以给槽位配
  任何逐槽参数都**必须按坐标配**，按名字配会把对家整边的剪切方向配成反的。
  节点 `_contentSize` 恰好等于贴图原始尺寸，可以拿它交叉验证。

# 牌面字的剪切

四家的预渲染牌面都是「上下边水平、侧边倾斜」的平行四边形（量四家贴图，上下缘斜率一律 0.000）。
所以字只需要在**屏幕空间、旋转之前**沿 y 剪一次 x，剪切量 `SideMeldSlot.shear` 就是那条侧边的
`dx/dy`。变换顺序是 `T · Shear · R(−euler.z) · S`，这样字的横轴恒水平、竖轴恰好和牌面侧边平行。

- 剪切量**从贴图自身量**，不要搬预制体 card 的 `skewX` / `skewY` 角度。cocos 的
  `_updateLocalMatrix` 把 skew 乘在旋转缩放矩阵**右侧**，屏幕斜率会被 `|sy/sx|` 放大。
  自家 card 是等比缩放（0.47/0.47），放大系数 1，量出来的牌面斜率和 `tan(skewX)` 几乎重合
  （−0.230 对 −0.231）——说明美术当年就是照着牌面调的。但左右两家和对家是非等比缩放，
  同一角度放大后就偏了：右家赖子 `sy/sx = 1.54`，13° 放大成 0.356，而贴图牌面只有 0.159,
  字比牌面歪一倍多，这正是左右两家「字看着没对齐」的来源。
- 量法：扫象牙面（`r > 150` 且 `|r − g| < 40`）每行的左右缘，去掉上下各 28% 的圆角，
  最小二乘拟合 `dx/dy`，左右缘取平均。
- 自家/对家**逐槽**取：剪切量就是牌离镜头轴的横向距离，自家从屏幕左 −0.230 平滑升到中线
  0.000、对家 +0.209 降到 0.000，这个渐变是真的，少了它字垂直、牌面斜，看着就歪。
- 左右两家整边取**中位数**（碰杠 −0.2 / +0.252，赖子 −0.203 / +0.161）：那边的牌都挤在同一侧，
  斜率本该恒定，逐张量出来的 ±0.05 抖动是小图圆角的噪声，逐槽取会让相邻两张的字各歪一点。
  碰杠和赖子的值不同是对的——两者的横向位置不同（右家碰杠在 x≈1100、赖子在 x≈950）。

# 甩出的赖子

用 `qj_{left,right,up,down}_lz_show`，**不是** `*_lz_show`。同一套路，每槽一张预渲染长方体
（`zqjlz*` / `yqjlz*` / `sqjlz*` / `xqjlz*`，共 12 张），槽位在 `LAIZI_OUT`。

- **禁止**用 `*_lz_show` / `*_lz_show_qjhz` / `er_*_lz_show`：那几组摆在牌河外侧的空地上，
  和实机差 50~80px。实机截图里赖子正落在 `qj_*` 的槽位上（自家 972.5/584.7、对家 391.5/88.1、
  左家 256.2/530.6、右家 948.0/143.6），也正是各家长条方格的最右端。
- 槽位坐标**直接照搬预制体**，不要拿桌布描边去反推：那圈边框（上 70/109、下 566/622、
  左右随透视斜着走）是个斜面而不是平铺的格子，左右两条只有 44px 宽，塞不进 71px 的牌，
  硬吸附会两边溢出。
- 预制体那 6 个槽是 **3 摞 × 2 层**：`card_1/2/5` 是三摞的底（唯一 x、y 都单调的三元组），
  `card_3/4/6` 分别摞在它们头上。**禁止**照搬这 6 个槽——第 4 张会堆起来。
  我们只留一排：起点用 `card_1`，格距取近排三张的**平均**步长，第 4 张按同一步长外推。
- **一排牌要沿着自己牌面那条边的方向走**，`x` **禁止**照搬预制体——那几个数是手摆的，
  右家第 1→2 张的步进斜率只有 0.119、左家第 2→3 张只有 0.158，各差出 1~1.7px，
  就是从自己视角看「上面那张比较靠右」。自家/对家横排，贴图上下缘水平，所以 `dy = 0`；
  左右两家纵排，`dx = 步进斜率 × 格距`，斜率见下条。
- 步进斜率取牌面**前进那一侧**边的斜率（`LZ_LEAD`：右家往右下走取右缘 +0.196、
  左家往左下走取左缘 −0.216），**不是**两侧平均的 `shear`。牌面是**梯形**不是平行四边形
  （近端更宽：右家左缘 +0.121、右缘 +0.196），整排只用一张贴图两侧就不可能同时接上；
  但两侧不对等——前进那一侧要靠后甩的盖住前一张，露出来就是一个探头的角，
  另一侧本来就该看见前面几张。取平均时前进侧每条缝还探 0.4~1.3px，取前进侧后只剩圆角的 0.36px。
- 方向对了之后整排统一用**一张**贴图 + 匀距最平（左家前进侧轮廓倒退 0px、右家 1px）。
  **禁止**交替用预制体的两个变体：`yqjlz1_1` 58×42 比 `yqjlz1_2` 57×40 大一圈，一大一小地排，
  边缘反而成锯齿。单张排下来字距自然也是匀的，不必再单独重排牌面字。
- 缩放提到整边一个值，别逐槽取：左家 `card_5` 自带 0.3884/0.48，和前两张的 0.35/0.4581 差 11%。
- 叠压就按数组顺序，后甩的压前一张（左右两家是往视角方向排，正好也是近的压远的）。
- 牌面字的剪切见上面「牌面字的剪切」一节：和碰杠共用 `SideMeldSlot.shear`，从贴图牌面量。
- 金罩要压在牌面字和「癞」角标**下面**：实机牌身是橄榄金，字仍是深绿、角标仍是亮蓝。
- 金罩剪影用 `laiziMask` 从贴图自身取象牙面（`r ≈ g`，绿边不算）；**不能**拿字形位图的
 边界铺，那会飘出牌面。
- 罩色 `LAIZI_TINT = #f2ed5f`：实机拿同一排的普通牌和赖子对比，比值是 (0.997, 0.975, 0.388)
 ——R、G 几乎不动，**只压蓝通道**。手牌、牌河、甩出的赖子共用这一个值，几处颜色才一致。
 **不要**用偏橙的 `#ffd56a`（G 压太多，红「萬」会变橙），也不要用 `#d9de5a`
 （那是把桌面环境暗部误算进了罩色）。因为只压蓝，罩在字上面也不伤字，深绿/黑/红都还在。
- `drawLaiziTint` 要 `fill()` 路径本身，**不能** `clip()` 之后再 `fillRect`：
 帧内在 `multiply` 下那次 fillRect 会被整个丢掉（之前一直是靠一层叠加的橙色 fill 在顶着，
 撤掉它金色就没了）。
- 「癞」角标是 card 的 `pz` 子节点，但贴图要取 `lz`——`pz.png` 是橙色的「痞」。
  它跟着 card 的缩放走，对家负缩放会自动把它翻到左下角，别另写一套。
- 甩牌**不加**发光：实机就是平铺在桌布上，光晕会糊掉桌面。
